import React from 'react'
import EditForm from "../EditForm";

export default function EditPage() {
    return (
        <div>
            <p>This is the page that contains the form</p>
            <div>
                <EditForm />
            </div>
        </div>
    )
}